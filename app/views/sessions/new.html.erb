<%= form_for :session, :url => login_url, :method => :post do |f| %>

	<%= f.label "Email address" %>
	<%= f.text_field :email %>

	<div class="actions">
		<br/>
		<%= f.submit "Log in" %>
	</div>

<% end %>